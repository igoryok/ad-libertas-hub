# EMBEDDED SERVER CONFIGURATION (ServerProperties)
server.port=8081
server.display-name=Calculator
server.session.timeout=30

# LOGGING
logging.level.org.springframework=ERROR
logging.level.com.cosi.javatest=INFO

# APPLICATION
calculator.formula=(x1 + 5) * x2 / x1